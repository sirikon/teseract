{
  "scripts": {
    "example": "cd example && ts-node -P ../tsconfig.json ../src/main.ts",
    "build": "tsc --project ."
  },
  "devDependencies": {
    "@types/node": "^16.4.10",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "esbuild": "^0.12.17",
    "esbuild-plugin-sass": "^0.5.2"
  }
}
